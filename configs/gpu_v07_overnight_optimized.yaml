world:
  height: 100
  width: 100
  patch_size: 5
  max_steps: 5000
  seed: 7073
recording:
  enabled: true
  record_every: 25
  metrics_path: results/gpu_v07_overnight/metrics.parquet
  zarr_path: runs/gpu_v07_overnight/run.zarr
visualization:
  enabled: false
  backend: none
debug:
  assert_invariants: false
raqic:
  enabled: true
  mode: gpu_full
  decision_policy: raqic
  fallback_on_backend_error: false
  full_gpu_strict: true
  full_gpu_transfer_policy: persistent_mirror
  full_gpu_execution_tier: persistent
  full_gpu_precision: mixed
  full_gpu_memory_policy: preallocate
  full_gpu_memory_safety_fraction: 0.80
  full_gpu_graph_mode: segments
  full_gpu_stencil_backend: auto
  full_gpu_fuse_scatter: true
  full_gpu_phase_mode: canonical_device
  full_gpu_recording_level_v07: patch_summaries
  full_gpu_visual_backend: none
