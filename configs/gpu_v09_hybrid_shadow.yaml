world:
  height: 10
  width: 10
  patch_size: 5
  max_steps: 4
  seed: 9003
recording:
  enabled: true
  record_every: 1
  zarr_path: runs/gpu_v09_hybrid_shadow/run.zarr
  metrics_path: results/gpu_v09_hybrid_shadow/metrics.jsonl
visualization:
  enabled: false
  backend: none
debug:
  assert_invariants: true
raqic:
  enabled: true
  mode: gpu_full_hybrid_audit
  decision_policy: raqic
  fallback_on_backend_error: true
  full_gpu_strict: false
  full_gpu_no_silent_fallback: false
  full_gpu_transfer_policy: hybrid_shadow
  full_gpu_execution_tier: persistent
  full_gpu_cpu_shadow_ticks: 2
  full_gpu_shadow_strict: true
  full_gpu_shadow_tolerance: 1.0e-8
  full_gpu_phase_mode: canonical_device
  full_gpu_fuse_scatter: true
  full_gpu_recording_level_v07: metrics_plus_events
  full_gpu_visual_backend: none
