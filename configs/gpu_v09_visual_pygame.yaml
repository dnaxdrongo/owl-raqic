world:
  height: 30
  width: 30
  patch_size: 5
  max_steps: 100
  seed: 9006
recording:
  enabled: false
visualization:
  enabled: true
  backend: pygame
  fps: 30
debug:
  assert_invariants: false
raqic:
  enabled: true
  mode: gpu_full
  decision_policy: raqic
  fallback_on_backend_error: true
  full_gpu_strict: false
  full_gpu_no_silent_fallback: false
  full_gpu_transfer_policy: persistent_mirror
  full_gpu_execution_tier: persistent
  full_gpu_visual_backend: pygame_copy
  full_gpu_render_every: 1
  full_gpu_visual_event_capacity: 4096
  full_gpu_visual_clutter_budget: 1024
  full_gpu_visual_adaptive_lod: true
  full_gpu_visual_max_slowdown_fraction: 0.15
  full_gpu_phase_mode: canonical_device
  full_gpu_fuse_scatter: true
